explication sur l'algo de courtois

le serveur va écrire les scores des joueurs
le client va lire les scores dans la mémoire

on protege le memoire avec une semaphore pour permettre une lecture sans trop de pb

pour permettre qu'une seule interaction dans le shmem, on la protege avec un semaphore
    -> la shmem des scores est protégée par l'algo de courtois 
    car il est plus optimisé quant à un accès de plusieurs lecteurs et le mutex est plus élaboré.
        ->avoir plusieurs lecteurs, c pg
        l'important c'est de n'avoir qu'un seul writer à la fois pour éviter les pb de concurrence

rc = readrs coun => incrementee par les procesus lecteurs
    ->le rc sera lui aussi une memoire partagée pour que chaque processs lecteur puisse l'incrémenter
	    -> elle sera protégée par un mutex simple pour n'avoir qu'un seul acces à la fois

     